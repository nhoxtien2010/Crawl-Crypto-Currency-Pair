<nav class="navbar navbar-expand-sm bg-light navbar-light">
  <!-- Brand -->
  <img src="<%=	image_url('logo.png') %>" height="100" width="300">


  <!-- Links -->
  <ul class="navbar-nav">

    <li class="nav-item">
      <a class="nav-link" href="#">View candles chart</a>
    </li>

    <!-- Dropdown -->
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
        Choose Exchange
      </a>
      <div class="dropdown-menu">
        <% Exchange.all.each do |ex|%>
           <%=	link_to(ex.name, home_index_path(exchange:  ex.short_name), method: :get, class: 'dropdown-item')%>
        <% end %>
      </div>
    </li>

    <li class="nav-item ">
      <% if user_signed_in? %>
         <%=	link_to('Sign out', destroy_user_session_path, method: :delete, class: 'nav-link')%>
      <% end %>
    </li>
  </ul>

  <div class="">
    <a><span class="glyphicon glyphicon-globe"></span>  <%= @exchange.name %></a>
  </div>
</nav>